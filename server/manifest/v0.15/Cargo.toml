[package]
name = "server"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "server"
path = "../../src/main.rs"

[dependencies]
bevy = { version = "0.15", default-features = false, features = [
    "bevy_remote",
    "serialize",
] }
serde = "1.0.217"
